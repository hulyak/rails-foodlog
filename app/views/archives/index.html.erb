<div class="columns is-centered mt-6">
  <div class="column is-half">
    <% @user_entries.each do |day, user_entries| %>
      <h3 class="is-size-4 has-text-weight-bold mb-4 mt-4 has-text-centered">Entries for <%= day %></h3>

      <nav class="level is-mobile">
        <div class="level-item has-text-centered">
          <div>
            <p class="heading">Calories</p>
            <p class="title"><%= user_entries.pluck(:calories).sum %></p>
          </div>
        </div>
        <div class="level-item has-text-centered">
          <div>
            <p class="heading">Protein</p>
            <p class="title"><%= user_entries.pluck(:proteins).sum %>g</p>
          </div>
        </div>
        <div class="level-item has-text-centered">
          <div>
            <p class="heading">Carbohydrates</p>
            <p class="title"><%= user_entries.pluck(:carbohydrates).sum %>g</p>
          </div>
        </div>
        <div class="level-item has-text-centered">
          <div>
            <p class="heading">Fats</p>
            <p class="title"><%= user_entries.pluck(:fats).sum %>g</p>
          </div>
        </div>
      </nav>


      <% user_entries.each do |user_entry| %>
        <%= link_to user_entry_path(user_entry) do %>
          <div class="card">
            <div class="card-content">
              <strong><%= user_entry.meal_type %></strong><br/>
              <%= user_entry.calories %> calories,
              <%= user_entry.proteins %>g protein,
              <%= user_entry.carbohydrates %>g carbs,
              <%= user_entry.fats %>g fat
            </div>
          </div>
        <% end %>
      <% end %>
    <% end %>

  </div>
</div>